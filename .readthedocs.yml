version: 2
build:
  os: ubuntu-22.04
  tools:
    python: "mambaforge-22.9"
conda:
  environment: environment.yml
sphinx:
  configuration: docs/source/conf.py

# Because we will probs have issues with graph-tool if we use req. will try conda instead
# python:
#   install:
#     - requirements: docs/source/requirements.txt